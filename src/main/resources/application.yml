---
spring:
  profiles: default
  
  datasource:    url: jdbc:mysql://dockerce01:3306/mysql
    username: root
    password: password

---
spring:
  profiles: docker
  
  datasource:
    url: jdbc:mysql://dockerce01:3306/mysql
    username: root
    password: ${docker-secret-mysql-password}
    
docker-secret:
  bind-path: /run/secrets